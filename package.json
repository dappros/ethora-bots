{
  "name": "ethora-bots",
  "private": true,
  "workspaces": [
    "packages/*",
    "bots/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "dependencies": {
    "@babel/helpers": "^7.26.10"
  }
}
